{
  "name": "@apolla/eval",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "eval": "tsx run.ts",
    "build": "echo 'eval: nothing to build'",
    "test": "echo 'eval: run via pnpm eval'"
  },
  "dependencies": {
    "@apolla/protocol": "workspace:*",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
