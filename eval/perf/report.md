# Apolla Work 性能基准报告

- 日期：2026-08-27
- 模型：mock（零模型延迟）—— 度量平台自身开销，排除 LLM 推理时延
- 运行环境：Darwin 25.6.0 · 10 vCPU · Node v25.9.0
- 任务形态：每任务多步（Write → Bash 真实子进程 → Write → Artifact），auto 模式自动放行

> 口径：mock 模型无网络/推理延迟，因此下列数字反映的是**平台调度 + 工具执行 + 事件下沉 + 进程创建**的开销基线；
> 生产环境单任务时延与吞吐主要由模型 TTFT 与生成速度决定。容器执行器层面的隔离/泄漏指标（PRD T-102）不在本进程内基准范围。

## 并发压测

| 并发数 | 总耗时 | 吞吐(任务/秒) | 单任务均值 | p95 | min | max | 失败 | 总事件 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 20 | 28.2ms | 710.3 | 23.7ms | 27.1ms | 19.3ms | 27.1ms | 0 | 500 |
| 50 | 63.8ms | 784.2 | 49.5ms | 61.1ms | 37.2ms | 61.6ms | 0 | 1250 |

说明：**总耗时** = 一批 N 个任务并发（Promise.all）的墙钟；**吞吐** = N / 总耗时；**单任务均值/p95** = 各任务自身端到端耗时（含并发争用）。

## 首个流式响应（平台开销下界）

- 20 并发下，平均「首个事件」到达：0.01ms；平均「首个流式增量(message.delta)」：0.89ms。
- 对照 PRD §1.6「任务首个流式响应」目标 M1 < 3s / M2 < 2s：mock 下平台侧开销为毫秒级，余量充足（真实值由模型 TTFT 决定）。

## 事件吞吐

| 场景 | 事件数 | 耗时 | 速率(事件/秒) |
|---|---:|---:|---:|
| 真实单任务（127 事件流水线） | 127 | 3.6ms | 35,547 |
| 纯 sink 分发 | 200,000 | 1.1ms | 176,756,518 |

## 对照 PRD §1.6 达标判断

| 指标 | 目标 | 实测 | 结论 |
|---|---|---|---|
| 单执行节点并发（M1） | ≥ 20 | 20 并发 全部成功，710.3 任务/秒 | ✅ 达标 |
| 单执行节点并发（M2） | ≥ 50 | 50 并发 全部成功，784.2 任务/秒 | ✅ 达标 |

---

运行：`pnpm --filter @apolla/eval exec tsx perf/run.ts`（可用 `PERF_LEVELS=20,50,100` 调整并发档位）。
